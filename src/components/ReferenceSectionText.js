export const ReferenceSectionText = ({children, className}) => {
    return (
        <div>
            <p className = {className}>{children}</p>
        </div>
    )
}

export default ReferenceSectionText
